#matrix is two dimentional list.
matrix=[
    [1,2,3],
    [4,5,6],
]
# to print a single element such as: print(matrix[0][0])
#for printing all the elements:
for row in matrix:
    for col in row:
        print(col)